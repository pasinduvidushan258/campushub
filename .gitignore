# Database credentials and connection config — must never be committed to version control
/config/database.php

# User-uploaded media files — too large for Git and environment-specific
/assets/uploads/

# macOS system metadata files — generated automatically by Finder
.DS_Store

# Windows thumbnail cache files — generated automatically by Explorer
Thumbs.db